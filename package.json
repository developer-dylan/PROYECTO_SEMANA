{
  "name": "my-spa",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "backend": "json-server --watch public/db.json --port 3000"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3",
    "vite": "^7.0.4"
  }
}
